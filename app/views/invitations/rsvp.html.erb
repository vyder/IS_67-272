<% title "RSVP Step 1 of 2 - Enter your Invite Code" %>

<%= form_tag({:controller => "invitations", :action => "invite"}, :method => "post") do %>
  <%= label_tag(:invite_code, "Invite Code:") %>
  <%= text_field_tag(:invite_code, @invite_code) %>
  <%= submit_tag("Enter") %>
<% end %>
